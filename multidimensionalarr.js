// A multidimensional array is an array inside another array.
 // Most commonly, we use 2D arrays (array of rows & columns).
//   var arr = [[1,2,3],[45,78,56],[34,67,88]]
//   console.log(arr[2][1]) // array of index 2 and in that array whoose index is 1.

 // 2d arrays  is an array of array
 // Think of it like a table with rows and columns.
 // // 2D array example
let arr = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
]; // This is a 3 × 3 array.

// how to access values
console.log(arr[0][1]); // 2
console.log(arr[2][0]); // 7
